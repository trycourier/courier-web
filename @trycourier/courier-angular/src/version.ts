// Kept in sync with package.json's "version". ng-packagr does not perform a
// Vite-style build-time `define`, so the user-agent version is sourced from this
// constant rather than a `__PACKAGE_VERSION__` global.
export const COURIER_ANGULAR_VERSION = "1.0.0";
